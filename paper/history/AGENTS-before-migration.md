# Manuscript rules

## Terminal-first research pivot (owner instruction, 2026-09-06)

For the current arbitrary-data task, `itpplasma/navier/PLAN.md` supersedes
the former pressure/quotient/defect/speed-shell/material-response task order.
The research reset is committed at
`7425786324179a88dd83f5bff9f5da3f438b38e3` in `itpplasma/navier`.
The former full PLAN is preserved there as
`research/history/PLAN-before-intrinsic-pivot-2026-09-06.md`.

**The old family is retired as the primary research route.** Do not resume
local refinement, create another conditional consumer, or add manuscript
sections merely because the old dependency graph contains that route.
Reopening it requires a genuinely derived input-only endpoint argument,
not another unknown critical norm, clock, or refinement parameter.

The new mathematical evidence is in the research repository:

- `research/evidence/2026-09-06-intrinsic-record-tangents.md`: a complete
  author derivation of nonconstant ancient NS/Euler tangent extraction
  and local Reynolds noncollapse without an assumed critical bound.
- `research/evidence/2026-09-06-viscous-mixing-falsifier.md`: a complete
  author derivation of an actual smooth periodic viscous family defeating
  blanket rigidity of the normalized record-tangent package, including
  both diverging backward domains and a vanishing normalized initial
  seminorm. It is not a counterexample to the fixed-input whole-space
  regularity target.

Both notes are independent-audit pending. They are research evidence, not
new audited manuscript theorems, and they do not complete NS-R3. The active
exploratory direction must retain fixed-input ancestry rather than assume
that every nonconstant ancient object is impossible. See the live PLAN's
ten-architecture screen, counterexample tests, and explicit stopping rules.

**Hold the mathematical manuscript unchanged during this strategic reset.**
No new proof-map labels, theorem promotions, formalization work, polishing,
or integration are justified by the two notes alone. The retained
conditional manuscript and graph are not retracted; their validity and
the choice of primary research route are different questions. This update
changes research instructions only. It is not a manuscript proof update.

For this task the owner's express authorization permits commits and pushes
in both repositories, including unsigned commits. Preserve concurrent edits
and use non-force updates. The new directive supersedes conflicting older
task-order and signing language below, not privacy or attribution rules.

## Retained manuscript and repository rules

The private GitHub repository `itpplasma/navier-paper` is the manuscript
authority. Work only in this repository and the private `../navier` dossier.
Fetch `origin` before editing and preserve remote edits. Build locally and
push signed commits to GitHub. The former Navier Overleaf project was deleted
at the owner's request on 2026-09-05; do not recreate it or use Overleaf.

`../navier/PLAN.md` is the sole live project state, and
`../navier/docs/proof-graph.yaml` owns dependency structure. Generate
`proof_map.tex` using `python3 ../navier/tools/generate_map.py`; do not invent
or manually change map claims. Keep every unproved hypothesis explicit.
Phase I awaits the user's kickoff; no formalization is authorized yet.

For mathematical changes, freeze the candidate revision and obtain an
independent scoped audit. Build both documents with `make`, reject undefined
references and overflow, inspect the rendered pages and PDF link destinations.
Document integrity checks do not prove mathematics. Commit explicit paths
and push private verified checkpoints. Do not commit generated PDFs, TeX
auxiliaries, third-party material, credentials, or caches. No public release,
contact, submission, or added authorship without explicit authorization.

Commit and tag signing use the global Git SSH defaults managed in chezmoi.
The signing key must be available in ssh-agent; do not bypass signing when it
is unavailable. Historical commit citations resolve through
`docs/history-signing/2026-09-05-map.json`.

## Current task authorization (2026-09-06)

The owner explicitly requested paper-proof work, updates to main documents,
and commits/pushes in `navier` and `navier-paper`, including unsigned
commits. This supersedes read-only and signed-only instructions above for
the present task. Existing formalization remains separately authorized;
no formal claim is promoted here. Keep the repositories private, preserve
concurrent edits, refresh before non-force pushes, and distinguish complete
derivations, author checks, independent audits, and unproved estimates.
