# Navier--Stokes: nonlinear paired refinement after the closed-feedback test

```yaml
terminal_claim: NS-R3
checkpoint: CP1
phase: coupled-refinement-research-2026-09-07
phase_i_status: reopened-2026-09-06-in-progress
phase_ii_status: reopened-2026-09-06-target
paper_status: conditional-manuscript-held-after-strategic-pivot
paper_repo: writable-authorized-2026-09-06
external_deps: permitted-if-no-axioms-beyond-mathlib
active_task: RF3-nonlinear-paired-refinement-with-controlled-block-amplification
active_architecture: compatible-refinement-with-controlled-restart-and-nonlinear-feedback-unproved
complete_terminal_route: none-established
terminal_status: not-proved
terminal_obstruction: input-only-critical-refinement-estimate-not-produced
closed_feedback_wave: entire-series-and-polynomial-cost-premises-refuted-restart-repaired
secondary_goal: MIC-R3
secondary_goal_status: kinetic-and-microscopic-interfaces-remain-separately-gated
formal_work_this_run: deferred
public_release: false
```

## 1. Authority and preservation

This is the sole live allocation. NS-R3 is unchanged: every solenoidal Schwartz
datum on R3 and every fixed nu>0, ORIGINAL unforced incompressible NS, globally
smooth velocity AND normalized pressure, with energy no greater than its
initial value. No periodic, kinetic, modified or finite-dimensional theorem
silently replaces it.

The previous PLAN at 49b67060edbda947f76cd1da290678096a415827 is preserved
byte-for-byte as `research/history/PLAN-before-closed-feedback-2026-09-07.md`,
blob `2ab606b06e7cc831ee557971fb4bf0f7d14859b3`. Its own predecessor archive
and every earlier result, scope and audit qualification remain intact.
The canonical terminal nodes/dependencies in `docs/proof-graph.yaml` are
unchanged; new exclusions and the research proof map are linked separately.
NS-R3 and CRITICAL remain gaps. No manuscript or formalization is promoted.

Read `research/refinement-slow-manifold-contract.md`, the current research
map `docs/refinement-programme.yaml`, and the detailed pending proof map
`docs/refinement-proof-graph.yaml`. The existing kinetic/Hilbert contracts,
KPC sources and `docs/programme-graph.yaml` still govern KIN-R3 and MIC-R3.

## 2. The surviving terminal-facing architecture

For one faithful sequence of nested conforming reconstructions with
h_j=2^(-j)h0, the sufficient output remains

    sum_j h_j^(-1/2)||u_(j+1)-u_j||_(L-infinity(0,H;L2))
        <= C(u0,nu,H) < infinity.                           (RF-SUM)

A uniform common-space inverse inequality, bounded coarse term, consistency,
initial trace, pressure/tail and energy passages then produce an R3 limit
bounded in L-infinity L3. Weak--strong identification and the banked
CONTINUATION/ENERGY suffix finish NS-R3. These interfaces remain explicit
and uninstantiated. RF-SUM is stronger than necessary, not an equivalent
reformulation advertised as progress; a weaker proved critical output with
its full consumer may replace it.

The active producer is a NONLINEAR estimate on the actual paired source,
feedback, amplification, saturation and reconstruction across levels. Retain
resolved corrections in the interlevel error, not just high modes. With v
coarse and U fine on the common fine space, w=U-v obeys exactly

    dot w+nu A w+B(v,w)+B(w,v)+B(w,w)=F,
    F=-(I-P_coarse)B(v,v),
    (1/2)d||w||2^2/dt+nu||grad w||2^2
        =<F,w>-integral (w.grad)v.w.

The source is generated by v; it is not an arbitrary forced stress. The
strain term is not skew. Estimating everything by an unknown strain integral,
small critical data, or an unbounded cutoff-dependent normal norm is not a
producer. A metric must include its time derivative and proved equivalence
constants. A restart must include spatial interlevel reconstruction costs.

## 3. What the closed-feedback wave actually proves

Full proofs are in:
`research/evidence/2026-09-07-closed-feedback-and-causal-radius.md` and
`research/evidence/2026-09-07-restarted-feedback-and-conditioning.md`.
The first proof was pushed at d8787ff4f65164e6339be673ad9f9646cd15bd37.
All are author derivations with independent mathematical audit pending.

The test is a genuine invariant sector of FULL spherical Fourier--Galerkin
NS for sqrt(5)K<=N<sqrt(8)K. Its normalized equations are

    x'=-x+yz, y'=-4y-xz, z'=-5z+xy.

Every nonlinear mode below the cutoff is retained. Two computed exterior
modes explain why it is NOT a continuum invariant ansatz or valid at all
larger cutoffs. Real energy E=x^2+4y^2+3z^2 satisfies E'<=-2E. The inviscid
energy/enstrophy pair has an explicit Nambu representation. All three
components feed one another, unlike the earlier triangular benchmarks.

A. The homogeneous zero-centered causal series for q(1,0,1) has finite
amplitude radius on every positive normalized horizon. Positive comparison
of the complexified coefficients with sec/tan proves it. All REAL trajectories
are global. This refutes the universal entire/factorial-series implementation,
not real regularity or all asymptotic/resummation methods.

B. Real causal restart repairs convergence. The block needs at most
max(1,ceil(2sqrt(E0))) input-selected charts, with a geometric tail and a final
infinite-time chart. The proof extends to ALL finite Galerkin modes, with
R_N=N sqrt(m_N)||u_N(0)||2/nu, explicit propagator and finite-precision bounds,
and at most max(1,ceil(2R_N)) charts. R_N deteriorates with cutoff, and the
fixed-box spectral gap cannot be imported into R3. Making each temporal
truncation error tiny is not a bound on two exact spatially projected flows.

C. About (0,A exp(-4s),0), two-way normal feedback has amplification at least
2^(-5/4)exp(A/8) at s=log(2)/4. Every nonincreasing quadratic metric equivalent
to the fixed norm has condition ratio at least 2^(-5/2)exp(A/4). Polynomially
conditioned universal normal-contraction repairs therefore fail.

D. The TRUE refinement source for common input (epsilon,A,0) is
(0,0,epsilon A exp(-5s)). Its source-linear error cost is at least
5*2^(-5/4)sinh(A/8)/A as epsilon->0. Even paired sources do not guarantee
polynomial source-linear costs. These are real Galerkin trajectories and
variations, not a forced-Stokes surrogate or an R3 singularity.

## 4. Pruning is exact, not a rejection of whole subjects

Retire the following universal premises only in their proved scopes:

- static C2 spectral slaving with zero quadratic defect (earlier resonance);
- prefactor-one normal contraction in the uncorrected mass norm (earlier test);
- an all-amplitude convergent single-center homogeneous causal series (A);
- polynomially conditioned dissipative metrics in the coupled block (C);
- polynomial source-linear bounds even for its true refinement residual (D).

Controlled real restart, justified nonperturbative summation, exponential or
nonlinear input-dependent costs, signed block estimates, kinetic/FEEC transfer,
and RF-SUM remain OPEN. A growing upper estimate is not proof that every
sharper estimate must grow. No new strict singularity reduction or terminal
critical estimate was obtained by this wave.

Preserve the graph-lift no-go only for artificial conormal singularities of
exact finite-observable Dirac graphs; it says nothing against a dynamical
low/high split. Preserve earlier entropy/covariance, forced stress, prepared
Fisher, packet-scaling, and single-channel exclusions with their distinct
premise classes. The earlier singularity consumers do not force recurrence,
axiality, Type I or bounded record blocks for arbitrary singularities.

## 5. Next distinct action and support lanes

Do not expand the completed low-dimensional examples as primary work. Estimate
the NONLINEAR total refinement error in the full equation of Section 2,
allowing that both true source amplification and metric conditioning can be
exponential locally. Prove how actual source size, saturation and transfer
between neighboring levels combine into a critical summable bound. Independent
worst-case operator norms multiplied over levels are not a substitute.

RF0's compatible model/whole-space adapter and RF4's summability remain open.
Use exact Fourier algebra to identify the cancellation before adding a FEEC
implementation. FEEC requires uniform commuting/reconstruction bounds in
addition to incidence identities; an AP kinetic limit at fixed mesh is not
spatial regularity. Kinetic elimination must retain collision invariants,
velocity tails and epsilon|k| dependence, with the existing model contracts.

Possanner/Burby-style reductions require controlled reconstruction and normal
stability, not convergence inferred from all finite expansion orders.
Stafford-style identities need domains, cofactor/inverse bounds and cutoff
remainders; finite matrices cannot satisfy [D,X]=I. Bjoerk-style descent needs
a proved decreasing quantity and unchanged dynamics. Kleinian filtrations do
not supply an ADE classification of fluid singularities. These support lanes
are neither proven terminal routes nor discarded by this wave.

## 6. Workflow and verification gates

Use the frontier-math workflow from krystophny/prompts (blob
`d32e4a8dcb1e5f06c3fc9b44620f6f4c8d0d562e`): freeze exact claim, inputs,
first gap, falsifier, forbidden inferences and check; repair the first failed
bridge before abandoning a mechanism. Retire only proved failures. Keep one
primary analytic task, and do not invent worker execution or independent audits.
Preserve the repository's delegation rules in AGENTS.md.

Checkpoint substantive proof waves, refresh main before each non-force push,
and stage explicit paths. No public release, outside contact, manuscript or
formal-repository write, or authorship change is authorized by this wave.
Canonical promotion requires independent source/statement/mathematical audits.
Run research/verify.py --research-only on a complete checkout when available;
report snapshot-only checks honestly. The focused standard-library oracle is
`research/check_closed_feedback.py`; tests do not certify universal PDE proofs.

Final handoff must distinguish architecture falsification, finite-cutoff repair,
strict singularity reduction, and NS-R3. This wave achieved the first two,
not the latter two. The remaining critical producer is open, not refuted.
