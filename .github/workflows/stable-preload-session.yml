name: Stable preload discriminator
on:
  push:
    branches: [research/navier-stable-preload-20260912]
permissions:
  contents: read
jobs:
  discriminate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Freeze exact discriminator
        run: cp research/check_stable_preload_balance.py /tmp/check_stable_preload_balance.py
      - name: Refresh authoritative main and run fast gate first
        run: |
          git fetch origin main
          git checkout -B checked-main origin/main
          python3 research/game/run.py fast
      - name: Run exact stable-preload discriminator
        run: |
          python3 -m pip install sympy
          python3 /tmp/check_stable_preload_balance.py
      - name: Structural verification
        run: |
          python3 research/verify.py --research-only
          git diff --check
