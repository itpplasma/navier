name: Reference interstage discriminator
on:
  push:
    branches: [research/navier-interstage-20260912]
permissions:
  contents: read
jobs:
  discriminate:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Freeze exact discriminator
        run: cp research/check_reference_interstage_exponent.py /tmp/check_reference_interstage_exponent.py
      - name: Refresh authoritative main and run fast gate first
        run: |
          git fetch origin main
          git checkout -B checked-main origin/main
          python3 research/game/run.py fast
      - name: Test reference exponent
        run: python3 /tmp/check_reference_interstage_exponent.py
      - name: Structural verification
        run: |
          python3 research/verify.py --research-only
          git diff --check
