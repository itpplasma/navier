name: Interstage adapter discriminator
on:
  push:
    branches: [research/navier-interstage-adapter-wall-20260912]
permissions:
  contents: read
jobs:
  discriminate:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Freeze exact discriminator
        run: cp research/check_interstage_single_label_wall.py /tmp/check_interstage_single_label_wall.py
      - name: Refresh authoritative main and run fast gate first
        run: |
          git fetch origin main
          git checkout -B checked-main origin/main
          python3 research/game/run.py fast
      - name: Run exact single-label adapter discriminator
        run: python3 /tmp/check_interstage_single_label_wall.py
      - name: Structural verification
        run: |
          python3 research/verify.py --research-only
          git diff --check
