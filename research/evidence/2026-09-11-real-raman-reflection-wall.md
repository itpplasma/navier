# Reality/reflection wall for the one-sided single-axis Raman lattice

Date: 2026-09-11. Repository input:
`itpplasma/navier@73b12e8855c4a09c15984e75bbd2078a42756577`.

**Status: exact scoped obstruction to the pointed one-sided slow-lattice
interpretation for real Navier--Stokes fields.** The single-axis common-Beltrami
checker at `4444101c02030acf6654dd477eded267e298301f` proves a strong exact
statement for the one-sided leading Raman translations generated by its five
listed shifts. For the terminal real-valued problem, however, every Fourier
module necessarily carries its reflected conjugate. The reflected pair is
itself the `(-l,-Q)` Raman module, so the effective real slow operator contains
both `+l` and `-l` translations. The resulting lattice is not pointed.

The exact algebra is frozen by
`research/check_real_raman_reflection_wall.py`.

This does **not** refute the Beltrami purifier mechanism. It removes the current
all-orders pointed-semigroup certificate as a sufficient real-field closure and
replaces it with a bidirectional/self-energy problem.

## 1. Reality forces the reflected Raman module

For one single-axis module write

    q=N Q+l/2,
    r=-N Q+l/2,
    Q=e1,
    l.Q=0.                                                 (1.1)

A real field with Fourier coefficients at `q,r` also contains the conjugate
coefficients at `-q,-r`. But

    -q=N(-Q)+(-l)/2,
    -r=-N(-Q)+(-l)/2.                                     (1.2)

Thus the reflected pair is exactly the same-helicity common-sphere Raman module
with parameters `(-l,-Q)`. If the original module produces the leading slow
translation

    k -> k+l,                                              (1.3)

its reality partner produces

    k -> k-l.                                              (1.4)

No choice of Fourier phase removes the reflected module while retaining a real
velocity field.

For the five shifts in the single-axis checker this changes the generator set
from

    {l_1,...,l_5}

to

    {+/-l_1,...,+/-l_5}.                                  (1.5)

In particular the monotone `y+z` argument and the universal pointed tail used
there do not apply to the real lattice.

## 2. Exact depth-two return to the clean target

For the Beltrami-constrained leading Raman symbol, omitting a common nonzero
scalar,

    R_(l,Q)(k,a)
      =(a.Q)(Q.(k+l)) P_(k+l) Q.                           (2.1)

The actual contaminated middle target is

    h=(z-2,-1,-1),                                        (2.2)

and its `e1` coefficient `a_x(z)` is nonzero at every root of the clean-return
polynomial; the checker proves this by exact polynomial gcd.

For the single-axis shifts `l_x=0`, put

    x=z-2,
    kappa=h+l.                                             (2.3)

The first leg is nonzero and points along `P_kappa e1`. Applying the reflected
module `(-l,-e1)` gives an exact second leg back to `h`:

    h -> h+l -> h.                                        (2.4)

Restoring the Leray denominators and the common factor `-2`, if the module
strength is `alpha_l`, the same-module return is

    -4 |alpha_l|^2 a_x x^2 c_l P_h e1,                   (2.5)

where

    c_l=(kappa_y^2+kappa_z^2)/|kappa|^2 >0.               (2.6)

For the five explicit shifts the denominator-free positive factors are

    68, 64, 72, 68, 64.                                   (2.7)

Therefore the reflected return is nonzero for every nonzero module. Its phase
factor is `alpha_l conjugate(alpha_l)=|alpha_l|^2`, so changing module phase
cannot remove the return. Contributions from the five same-module loops carry
the same geometric sign factor.

This is stronger than a merely possible collision: the current real purifier
has an unavoidable phase-independent self-energy path into the clean target
window at depth two.

## 3. Consequence for the preceding single-axis certificate

The checker at `4444101...` established, for the one-sided generator set,

* full `Sym_0(3)` span of the intended first outputs;
* exact inactivity of the old `p1`, `g1`, and inherited-ladder polarizations;
* 27,512 finite damping certificates;
* a pointed coercive tail; and
* no return to required windows after the intended first outputs.

The first two statements remain useful local algebra. The last three are not a
complete certificate for a **real** high background because the reflected
translations were absent from the tested semigroup. Equation (2.4) supplies an
explicit required-window return already at depth two.

The obstruction is scoped: it does not show that the return is destabilizing.
It may be possible to absorb the reality-paired loops into a renormalized slow
self-energy and then prove stability of the remaining bidirectional lattice.
But that is a different normal-form problem and must be solved before claiming
all-orders real-field purification.

## 4. Recomputed frontier

The next discriminator is now more specific than the previous generic
fast-lattice question. Work with the **reality-complete single-axis Beltrami
background** and derive its exact slow Schur complement including both signs of
every Raman shift.

Required outcomes:

1. sum the unavoidable `+l,-l` same-module loops and identify the exact diagonal
   self-energy on `h0` and the other active clean carriers;
2. determine whether that self-energy can be incorporated into the desired
   inheritance filter without destroying the clean target margin;
3. include mixed `+l_i,-l_j` loops and test for a finite invariant unstable
   block or a symmetrizer;
4. only after the bidirectional slow block is controlled, return to the
   off-shell high-sideband/Krein lattice and thin-shell localization.

If the real Schur complement has an exact positive-growth invariant block at
order one, the current single-axis Beltrami realization fails. If it admits a
bounded symmetrizer after renormalizing the diagonal loops, the reality wall is
repaired and the high-sideband normal form becomes first again.

No PLAN/canonical proof-graph promotion, recursive turnover, finite-time
singularity, or `NS-R3` conclusion is claimed here.
