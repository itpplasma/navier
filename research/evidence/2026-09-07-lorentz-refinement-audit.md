# Audit record for finite Lorentz refinement

Date: 2026-09-07. Actual independent reviews, recorded by the controller.
Base `ec2ed88c8d1fdf3b66b0e989827d44d93f1f76df`; owner-authorized parallel
frontier models. The mathematical target remains open. Component proof,
source inspection and independent integration review have separate scopes.

## Frozen component inputs

- Synthesis author input: `bbe63f4181c391c47a19da37a1c31313e24c259760f3b5996fc011a815c38628`.
  Fresh-context worker `audit_lorentz` returned PASS on shell synthesis,
  overlapping balls, time transfer, L2 tail and the conditional producer
  identity. It did not certify production or the continuation source.
- Continuation author input: `b89d680f1375edd07ff53c3067de985fff6b986d5433c99b7e50240fe8b1b9f5`.
  Independent `audit_construction` reviewed the distinct source/application
  scope and returned PASS after inspecting Phuc's actual statements.
  The source ledger was clarified: L^{3/2} pressure belongs to suitability,
  while Theorem 1.5 states the L2_t L1_x pressure hypothesis. The proof
  supplies both. This was a metadata correction, not a failed proof bridge.

Full component reports follow below. They precede final integrated wording,
which explicitly defines Wq_M and every time/index range. The final synthesis
and continuation files receive their own combined integration review.

## Frozen combined review input

- `2026-09-07-lorentz-synthesis.md`, SHA256
  `15b97fb3bf57d04677c928a8448d26a1d07fdfc61921501715a7134835305384`.
- `2026-09-07-lorentz-continuation.md`, SHA256
  `77444642d9c726749721101422386a222ed322bddaeea0b6c86f056bcfe8b301`.
- Exact concatenated new-file patch SHA256
  `e5d0737ee834cf76126cf694fbcdabaeb89e6d9dae2054346be5f3e005294ae2`.

The concatenation order is synthesis then continuation, each generated by
`git diff --no-index -- /dev/null <path>`. The proof contents and base commit
make these uncommitted review inputs reproducible. The combined audit
checks RF-q for one fixed finite q>3 on every finite horizon through
synthesis, local identification, Phuc's local results and canonical
continuation to the original velocity, pressure, trace and energy target.
RF-q production is an explicit unproved premise throughout.

Integration verdict: PASS. Fresh-context `audit_integration` found no
invalid implication in the full two-file composition, independently checked
Phuc's primary statements, and verified both file hashes against the exact
combined patch. No required mathematical repair. The full report below
has SHA256 `2efbb43298219e113875f108bb3bc6925f28aad0b7099fb42786d9f2ecd35645`.
It certifies the conditional consumer only; RF-q remains unproved.

## Independent synthesis review

The following report is retained verbatim from `audit_lorentz`; original
SHA256 `1aced0ff2aa92d63b4628f75368c3c2dac5bfd3fd8bfb2c3b73d2883be53543c`.

# Independent mathematical audit: finite Lorentz refinement

VERDICT: PASS

REVIEWED SCOPE: the complete frozen author packet
.git/navier-wave-20260907/lorentz-synthesis.md, SHA256
bbe63f4181c391c47a19da37a1c31313e24c259760f3b5996fc011a815c38628,
at declared base ec2ed88c8d1fdf3b66b0e989827d44d93f1f76df.
The digest was verified before and after mathematical review.
A fresh Codex subagent performed this audit under the current owner override
of the older Claude-only rule, as relayed by the controller. This is a
mathematical audit, not source verification or formal verification.

The reviewed claims are (L1), (L2), the conditional RF-q bound on actual
projected approximants and the original classical branch, the L2 tail
estimate, and the finite-sum production identity and its stated sufficient
integrated bound, for fixed 3 <= q < infinity. Production of RF-q and the
separate Lorentz continuation theorem are outside the passing scope.

FIRST BAD BRIDGE: none in the reviewed claims. The first unresolved
terminal-facing estimate remains production of RF-q; the finite-q
continuation dependency also remains subject to its separate audit.
Neither is covertly proved by this packet.

EVIDENCE:

1. Shell estimate and dyadic indexing.

For a finite shell sum write f = l_J + h_J. Fourier inversion and
Cauchy--Schwarz give ||l_J||infinity <= A_J; orthogonality gives
||h_J||2^2 = T_J. The distribution inclusion

    {|f| > A_J + beta} subset {|h_J| > beta}

and Chebyshev give f*(s) <= A_J + s^(-1/2) T_J^(1/2).
This uses the Euclidean vector norm and needs no rearrangement triangle
inequality. Multiplication by s_J^(1/3) = lambda_J^(-1) gives precisely
the stated d_J, including the high-frequency weight 2^(J-k).

On [s_(J+1), s_J], monotonicity gives f*(s) <= f*(s_(J+1)), while
s^(1/3)/s_(J+1)^(1/3) <= 2. Thus 2 d_(J+1) is in the correct direction.
The intervals partition (0,infinity), each with ds/s measure log 8.
The convolution kernels have sums 2 for the low term and 1 for b^2 in the
high term. Young on ell^(q/2) is valid since q/2 >= 1; Minkowski on ell^q
then gives exactly C_q = 2(log 8)^(1/q)(2B+1).

2. Low frequencies and Lorentz Fatou.

Symmetric truncation of the integer shell indices converges to every L2
function in L2. The only omitted frequency is zero, a Lebesgue null set.
A spatial almost-everywhere subsequence exists. Layer cake gives

    integral s^(q/3-1) f*(s)^q ds
      = q integral alpha^(q-1)
          [integral_0^(measure{|f|>alpha}) s^(q/3-1) ds] d alpha
      = 3 integral alpha^(q-1)
          measure{|f|>alpha}^(q/3) d alpha.

The prefactor is exactly 3. Almost-everywhere convergence implies
measure{|f|>alpha} <= liminf measure{|f_n|>alpha} for every alpha > 0.
Fatou in alpha proves the required lower semicontinuity. The shell ell^q
norm of each truncation is bounded by the full norm. Arbitrarily low
frequencies are covered; there is no spectral-gap assumption or Lorentz
ball-multiplier estimate.

3. Ball overlap and the coarse term.

For support |xi| <= N_(j+1), Delta_k f_j vanishes when k > j+1.
At k = j+2, the shell's strict lower endpoint is the ball radius, so no
boundary contribution was missed. On the remaining indices, contraction
gives lambda_k^(1/2)/N_j^(1/2) = 2^((k-j)/2). The admissible range is
therefore j >= k-1, and the kernel sum is
sqrt(2)/(1-2^(-1/2)). Only L2 and ell^q triangle inequalities are used.
Arbitrarily overlapping low corrections satisfy the argument.

At a fixed time include the actual projected u_0(t) as f_(-1), taking
the auxiliary N_(-1)=N0/2. It has the required Fourier support, and energy
gives c_(-1)(t) <= (N0/2)^(1/2)||d||2. A single application of (L2) to
this coarse term and the increments gives exactly the displayed qth-root
bound. No Lorentz quasitriangle constant is silently dropped.

4. Actual time quantifiers and identification.

The referenced family is defined in sections 1, 2, and 4 of
research/evidence/2026-09-07-whole-space-cubic-refinement.md.
The consulted file had SHA256
5e985a4587264d20650a6038132e0dd415ef223c3f2409f027f7b1a02481b31b.
RF-q is read with 0 <= t <= H, integer M >= 1, and 0 <= j < M, as in
that family's RF-CUBE definition. These ranges should be written explicitly
when integrating the packet for clarity.

Each finite sum of qth powers is continuous in time by L2 continuity of
the actual projected paths. Its essential bound therefore holds at every
time, including the finite interval endpoints. Countably many finite sums
share the bound; there is no uncountable null-set problem. The same synthesis
estimate on a single fixed band also gives continuity of each projected
path into L^{3,q}, if needed.

Accepting the separately reviewed compact-classical L2 identification
premise, fix any t < min(H,Tstar) and a compact classical interval
containing t. The approximants converge to u(t) in L2. A subsequence,
allowed to depend on that fixed t, converges almost everywhere in space;
Lorentz Fatou gives the same input-only bound on u(t). Neither a common
subsequence for all times nor uniform Lorentz tail convergence in time is
needed. Constants from compact-classical comparison do not enter the
RF-q bound. Thus the same bound holds at every classical time before the
endpoint.

5. L2 tail and production identity.

Hölder on sum_(j=L)^(M-1) 2^(-j/2) a_j gives exactly

    N0^(-1/2) 2^(-L/2)
    (1-2^(-q'/2))^(-1/q') K_q,

uniformly in time and M>L, for q>1.

Define Wq_M = sum_(j=0)^(M-1) N_j^(q/2)||e_j||2^q.
The derivative of the qth Hilbert norm and the exact paired energy identity
give (1/q)Wq_M' + nu Dq_M = Piq_M. At e_j=0, the derivative and displayed
terms vanish for q>=3, so the identity has no division-by-zero gap.
The initial shell errors obey

    N_j^(1/2)||e_j(0)||2
      <= (2pi)^(-1) N_j^(-1/2)||grad d||2,

whose qth powers have a convergent geometric sum. If the proposed integrated
bound holds at every upper time uniformly in M, integration gives

    Wq_M(t) <= Wq_M(0) + q C.

Strict absorption is therefore unnecessary for this supremum certificate.
A bound only at the final time would be insufficient; the packet correctly
requires every upper time. The definition of Wq_M should be explicit in
the integrated version, though its meaning is clear from the stated identity.

REPLACEMENT ARGUMENT: none needed.

CONDITIONAL SUFFIX THAT SURVIVES: RF-q gives the stated uniform L^{3,q}
bound on actual approximants and, using the accepted L2 identification,
the original classical branch at every classical time. If a separately
verified finite-q continuation theorem applies with this bound, the
conditional continuation consumer is valid.

UNNECESSARY DEPENDENCIES: the spatial synthesis requires neither the PDE
nor solenoidality. Its argument works for finite q>=2, although the
requested claim and conditional PDE consumer use q>=3. No Lorentz-space
ball-multiplier theorem, Lorentz quasitriangle estimate, uniform Lorentz
convergence in time, expanding-box limit, or weak-solution uniqueness
theorem is needed for the reviewed transfer.

NON-CLAIMS: no RF-q producer, no source or applicability audit of the
imported continuation theorem, no q=infinity continuation, no terminal
NS-R3 proof, no novelty finding, no canonical promotion, and no formal
certification. The sequence-space implication RF-CUBE => RF-q for q>3
does not prove that actual NS trajectories satisfy either certificate.

REOPENING CONDITION: changed hypotheses or estimates require review of
the affected implication. A terminal claim additionally requires the
missing producer and the separately gated continuation applicability audit.


## Independent continuation source and application review

The following report is retained verbatim from `audit_construction`;
original SHA256 `aa01dbe15f7b29d512ec2b95040743c90f3a2bdf987b70897fd72fdb2e70e988`.

# Independent source and finite-endpoint adapter audit

VERDICT: PASS

REVIEWED SCOPE: the claim that one fixed finite q>3 bound in
L-infinity(0,Tstar;L^{3,q}(R3)) on the canonical Schwartz-data classical
branch excludes a finite maximal time. This includes the actual Phuc local
theorem interfaces, pressure construction, suitable class, time boundary,
far-field estimate, finite cover and composition with canonical CONTINUATION.
No refinement synthesis or arbitrary-data Lorentz producer is reviewed.

Frozen base: `ec2ed88c8d1fdf3b66b0e989827d44d93f1f76df`.
Frozen candidate: `lorentz-source.md`, SHA256
`b89d680f1375edd07ff53c3067de985fff6b986d5433c99b7e50240fe8b1b9f5`,
checked before reading. The candidate was not edited.
The candidate hash was rechecked after the completed audit and was unchanged.

FIRST BAD BRIDGE: none. One nonblocking source-ledger refinement is recorded
below; it does not remove a hypothesis available in the proof.

EVIDENCE:

1. **Independent primary-source inspection.** I read the relevant statements
   from Phuc's [author PDF](https://www.math.lsu.edu/~pcnguyen/papers_html/Phuc_NS_regularity_09_06_15_Simplify_3D.pdf)
   and cross-checked the explicit upper-boundary notation in the
   [arXiv v2 HTML](https://arxiv.org/html/1407.5129). Theorem 1.5, printed
   p4, requires local velocity energy class (1.6), pressure in L2_t L1_x,
   distributional unforced unit-viscosity NS, and finite q>3 Lorentz control.
   It concludes velocity Holder continuity on the closed smaller cylinder.
   Proposition 3.2, pp10--11, requires suitability and small C(8)+D(8), and
   bounds every spatial velocity derivative there by universal constants.
   Definitions 2.1 and the preceding C,D formulas, pp6--7, match the packet.
   The HTML overlines in both conclusions explicitly include the upper time.
   These are imported statements whose applicability is audited, rather than
   independent reproofs of Phuc's regularity theory. No PDF was stored.

   Source-ledger refinement: Theorem 1.5 does not separately list spacetime
   L^{3/2} pressure; that requirement occurs in Definition 2.1 for suitability.
   Remove it from the theorem's attributed premise list and retain it in the
   suitability check. The candidate establishes both classes, so its stated
   sufficient version and subsequent application are valid as written.

2. **Lorentz convention and elementary estimates.** With the distribution
   convention in the candidate, integrating from alpha/2 to alpha gives

       ||f||_(3,q)^q
           >= [3(1-2^(-q))/q] alpha^q d_f(alpha)^(q/3).

   Taking the supremum proves the exact c_q in (1). The two distribution
   bounds `d_f(alpha)<=K^3 alpha^-3` and `d_f(alpha)<=D^6 alpha^-6` meet at
   alpha0=D^2/K. Their layer-cake contributions to ||f||4^4 are respectively
   4K^2D^2 and 2K^2D^2. Thus (2) is correct, including zero-norm cases.
   For the canonical branch all measurability requirements follow from its
   classical time dependence on compact intervals; the hypothesis is an
   essential-time bound, as required by the local theorem.

3. **Viscosity and endpoint energy.** The stated change
   `s=nu t`, `v=u/nu`, `pi=p/nu^2` gives
   `v_s-Delta v+(v.grad)v+grad pi=0`. Its initial energy is
   `nu^(-2)||u_initial||2^2`, and its total gradient integral is bounded by
   half that energy. Monotone convergence of the original exact energy
   identities supplies this integral up to Sstar without an endpoint trace.
   The Lorentz norm changes by the stated factor nu^(-1); the spatial domain
   remains R3. All viscosity powers check.

4. **Canonical pressure is in the required class.** Sobolev plus item 2
   gives v in L4 over the entire finite spacetime slab. The pressure tensor
   multiplier has Frobenius norm one, so

       ||pi(s)||2 <= ||v(s) tensor v(s)||2 = ||v(s)||4^2

   and pi is globally L2 in spacetime. This uses the canonical pressure
   normalization; an arbitrary spatial constant depending on time would
   not have this global property. No such constant is added here. On bounded
   cylinders, finite-volume Holder supplies L^{3/2} spacetime pressure and
   L2_t L1_x pressure. No non-L2 Riesz multiplier theorem is needed.

5. **Local hypotheses hold on a cylinder ending at Sstar.** Global energy
   supplies the local L-infinity L2 and L2 W^{1,2} classes. Compactly
   supported distribution tests in the open cylinder stay strictly before
   Sstar, where the classical equation holds. For each upper test time
   t<Sstar, the local energy equality follows by classical integration,
   since the nonnegative test vanishes near the bottom and lateral boundary.
   The required terms are integrable over the full cylinder: v is L4,
   pi is L2, and the gradient is L2. This gives the suitable class used in
   Proposition 3.2; its definition asks for the inequality at almost every
   time inside the open interval, not at or beyond Sstar. No global
   Leray-Hopf extension or endpoint solution value is presumed.

6. **Far-field control is uniform in space and time.** The tail F(R) tends
   to zero by global spacetime integrability. With a=8r fixed as in the
   packet, all cylinders centered at |x0|>R+a and
   s0 in [Sstar/2,Sstar] lie in the spatial tail and strictly above initial
   time. For each time slice, Holder gives

       ||v||_(12/5,B_a)^4 <= |B_a|^(2/3)||v||_(4,B_a)^4,
       ||pi||_(6/5,B_a)^2 <= |B_a|^(2/3)||pi||_(2,B_a)^2.

   Multiplication by a^(-3) gives exactly
   `C+D <= |B1|^(2/3) a^(-1) F(R)`. There is one R making this at most
   epsilon1 for all centers and upper times. Under V=r v and P=r^2 pi,
   both C and D are invariant and the cylinder maps to Q8. Proposition 3.2
   therefore gives the same bound C0/r at every center for s0<Sstar.
   Continuous representatives agree with the original classical field at
   these times. The argument needs a spacetime tail, not uniform-in-time
   Lorentz-tail smallness. It prevents possible unbounded velocity from
   escaping to spatial infinity near Sstar.

7. **Bounded-region and upper-time conclusion.** Restriction to any ball
   decreases the Lorentz distribution function. Critical parabolic scaling
   preserves the L^{3,q} velocity norm. Therefore the uniform Lorentz
   hypothesis and item 5 allow Theorem 1.5 on each Q_rho(x0,Sstar).
   The theorem supplies a Holder representative on the closed smaller
   cylinder, hence a finite bound up to the upper time. On times below
   Sstar it equals the classical branch by continuity and almost-everywhere
   equality. A finite cover of the remaining compact spatial ball gives
   one finite bound for the common interval
   `[Sstar-rho^2/4,Sstar)`. Its constants need not be uniform before taking
   this finite cover. This interval lies in the time range of the far-field
   estimate, so both pieces combine into a global spatial Linfinity bound
   near Sstar. Earlier times are covered by canonical LOCAL.

8. **The exact consumer is available.** Energy and the resulting Linfinity
   bound imply `||v(s)||3^3 <= ||v(s)||infinity ||v(s)||2^2` uniformly up to
   Sstar. Undoing viscosity scaling yields the uniform L3 bound for u on
   `[0,Tstar)`. The canonical CONTINUATION node explicitly contradicts this
   when the Schwartz-data maximal time is finite. Its conclusion and LOCAL
   then carry the canonical normalized pressure as well as velocity. The
   proof does not misread velocity Holder regularity as a pressure theorem.

REPLACEMENT ARGUMENT: none is required for the mathematical chain. Apply the
source-ledger wording correction in item 1 when integrating the evidence.

CONDITIONAL SUFFIX THAT SURVIVES: a correctly established fixed finite q>3
bound on this classical branch feeds CONTINUATION through the complete
pressure, tail and covering argument above. A synthesis or identification
theorem that supplies that bound requires its own independent audit.

UNNECESSARY DEPENDENCIES: Phuc's global Theorem 1.7 and its global
Leray-Hopf definition are unnecessary for this adapter. Barker--Seregin is
not used and is not separately certified here. Compact support of the
Schwartz datum is not assumed. No global weak extension, uniform-in-time
Lorentz-tail statement, weak-L3 pressure multiplier, or explicit quantitative
bound for the covering radius is needed. Only k=0 of Proposition 3.2 is used.

NON-CLAIMS: this is a qualitative conditional continuation theorem for each
fixed finite q>3, not a bound as q tends to infinity. The weak-L3 endpoint
is not covered by the local imported theorem. No arbitrary-data Lorentz
bound, refinement certificate, source novelty, NS-R3 proof, formal
verification or canonical promotion follows from this audit.

REOPENING CONDITION: no failed mathematical implication was found. Applying
the same conclusion to a different solution class, pressure normalization,
domain, endpoint q=infinity or synthesis output would require a new
applicability check.

## Independent full integration review

# Independent integration review: finite-Lorentz refinement and continuation

VERDICT: PASS

REVIEWED SCOPE:

The complete two-file candidate, including the finite-q synthesis constant,
overlapping resolved corrections, RF-q and its producer identity, transfer
to the classical branch, the Lorentz convention, pressure and suitability,
the finite-lifespan local-theorem application, spatial tails, and final
continuation for the original NS-R3 target. This review reconstructs the
composition independently; component reviewer agreement is not evidence
for a mathematical implication.

Frozen base: `ec2ed88c8d1fdf3b66b0e989827d44d93f1f76df`.

- `research/evidence/2026-09-07-lorentz-synthesis.md`, SHA256
  `15b97fb3bf57d04677c928a8448d26a1d07fdfc61921501715a7134835305384`.
- `research/evidence/2026-09-07-lorentz-continuation.md`, SHA256
  `77444642d9c726749721101422386a222ed322bddaeea0b6c86f056bcfe8b301`.
- Combined exact new-file patch
  `.git/navier-wave-20260907/lorentz-integration.patch`, SHA256
  `e5d0737ee834cf76126cf694fbcdabaeb89e6d9dae2054346be5f3e005294ae2`.

The previously reviewed direct whole-space projected family, its compact
classical L2 comparison, and canonical LOCAL, ENERGY and CONTINUATION
are accepted scoped inputs. Their literature proofs are not recertified.

FIRST BAD BRIDGE: none in the reviewed implication.

EVIDENCE:

1. The Lorentz conventions agree exactly.

For finite q, layer cake followed by Tonelli gives

    integral_0^infinity s^(q/3-1) f*(s)^q ds
      = q integral_0^infinity alpha^(q-1)
                   integral_0^d_f(alpha) s^(q/3-1) ds d alpha
      = 3 integral_0^infinity alpha^(q-1) d_f(alpha)^(q/3) d alpha.

Consequently the synthesis rearrangement quasinorm and the continuation
distribution quasinorm coincide, including the factor 3. Neither a
normalization factor nor the secondary exponent is changed at integration.
The Fourier convention is exp(-2 pi i x dot xi) throughout the synthesis;
B=sqrt(4 pi/3) is the corresponding ball-volume constant.

2. The finite-q spatial synthesis is valid with its stated constant.

Chebyshev applied to the high part and the low Linfinity bound yield
f*(s) <= A_J + s^(-1/2) T_J^(1/2). At s_J=lambda_J^(-3), multiplying
by s_J^(1/3) produces exactly the two terms d_J in the candidate.
For s between s_(J+1) and s_J, monotonicity of f* and
s_J^(1/3)=2 s_(J+1)^(1/3) give the factor 2. These intervals partition
(0,infinity) and have logarithmic length log 8.

The low convolution has ell1 norm 2; its coefficient B gives 2B.
The squared high convolution has kernel sum_(n>=1)2^(-n)=1.
Young on ell^(q/2), followed by the square root, bounds its ellq norm
by ||b||ellq. Minkowski for these two nonnegative sequences proves
C_q=2(log 8)^(1/q)(2B+1). The required q/2>=1 holds in the claimed
q>=3 range. Both shell extremes can be truncated in L2. For an
almost-everywhere convergent subsequence, d_f(alpha)<=liminf d_fn(alpha)
for every positive alpha, so the displayed distribution formula and
Fatou supply the general L2 conclusion.

Sharp-shell contraction and the triangle inequality in L2 give the
overlapping-increment convolution, whose ell1 norm is
sqrt(2)/(1-2^(-1/2)). No Lorentz triangle inequality or sharp-ball
multiplier theorem is being silently used. Including the coarse term
as index -1 is valid: its allowed radius is N0 and its weighted L2
size is (N0/2)^(1/2)||u_0||2. Thus the finite-approximant bound includes
all resolved corrections and its displayed coarse contribution is correct.

3. Time and cutoff quantifiers survive transfer to the original solution.

Choose one fixed finite q>3, for example q=6, before taking the refinement
limit. RF-q bounds every finite partial sum at every time with one K_q
for the chosen datum, viscosity, N0 and horizon. Continuity of the
countably many finite partial sums makes an essential-time formulation
equivalent for the actual smooth projected paths. No sum of levelwise
time suprema is substituted for the stated simultaneous-time sum.

For each T<Tstar, the accepted comparison gives u_(N_j)->u in C_t L2_x.
At each fixed classical time, a spatial almost-everywhere subsequence
and Lorentz Fatou transfer the SAME finite-approximant bound to u.
The comparison constants may depend on T and the classical H3 norm;
they never enter the transferred bound. Taking increasing compact
classical intervals therefore preserves the bound below min(H,Tstar).

There is no hidden time-measurability requirement left open for this
actual classical branch. On compact classical intervals LOCAL gives
C_t H3, hence C_t L2 and C_t Linfinity. The distribution formula implies

    ||f||_(3,q) <= (9/q)^(1/q) ||f||2^(2/3)||f||infinity^(1/3)

for f in L2 intersect Linfinity, by integrating
d_f(alpha)<=||f||2^2 alpha^(-2) up to ||f||infinity. Applied to time
differences, this proves continuity in the Lorentz topology. Countably
many compact intervals give the required measurable path. Uniform
Lorentz convergence of approximations in time is unnecessary.

The additional L2 tail follows by Holder with q and q'=q/(q-1),
with precisely the geometric constant stated in the synthesis file.
It is not needed to construct a separate weak limit for this consumer.

4. The inspected local source is applicable at the required strength.

I independently opened the primary [Phuc arXiv v2 text](https://arxiv.org/html/1407.5129).
Its Lorentz definition uses the same distribution factor p, hence 3 here.
Theorem 1.5 assumes distributional unit-viscosity unforced NS, the stated
local velocity energy class, pressure in L2_t L1_x, and fixed 3<q<infinity.
Its conclusion includes the closed smaller cylinder. Definition 2.1
requires spacetime L^(3/2) pressure and a local energy inequality for
suitability. Proposition 3.2 applies to suitable solutions on Q8 under
small C(8)+D(8), where C and D are the displayed 12/5 and 6/5 quantities,
and bounds velocity on the closed Q_(1/2). These are accepted published
results; their underlying proofs are not rederived here.

Thus neither the q=infinity endpoint nor an interior-only theorem is
substituted for the required statement. The global Theorem 1.7 and the
Barker--Seregin corroboration are not premises of the selected adapter.

5. Viscosity normalization and pressure introduce no equation change.

For s=nu t, v=u/nu and pi=p/nu^2, all four terms of unit-viscosity NS
equal nu^(-2) times their original equation terms. The spatial domain
is still R3, the datum is solenoidal Schwartz, and pressure normalization
is preserved. The energy and integrated-gradient bounds scale to the
displayed initial energy ||d/nu||2^2. Monotone convergence supplies the
dissipation bound up to Sstar without an endpoint value.

The finite-q-to-weak-L3 estimate follows by restricting its distribution
integral to [alpha/2,alpha], giving exactly c_q. For weak L3 size K and
L6 size D, splitting the L4 layer cake at D^2/K yields respectively
4K^2D^2 and 2K^2D^2. Thus the Sobolev and energy inputs imply global
spacetime L4 velocity on the finite hypothetical lifespan.

The normalized pressure symbol has tensor Frobenius norm one away from
xi=0. Pointwise tensor Cauchy--Schwarz and Plancherel yield
||pi||2<=||v tensor v||_(2,Frobenius)=||v||4^2, with no missing 2 pi
factor because the two derivatives cancel the inverse-Laplacian factor.
This proves global spacetime L2 pressure. The proof uses the canonical
classical pressure, not approximate pressures or a new limiting pressure.

6. Both local solution classes hold on endpoint-ending open cylinders.

On every bounded cylinder inside (0,Sstar), including one whose upper
endpoint is Sstar, energy supplies L-infinity_t L2_x and L2_t W1,2_x.
Finite-volume Holder applied to pi in spacetime L2 supplies both
L2_t L1_x and spacetime L^(3/2). Distributional tests have compact
support strictly before the upper time. The original solution is
classical there, so the distributional equation holds.

For a suitable-solution test vanishing near the parabolic boundary,
the classical local energy identity can be integrated up to each
t<Sstar. All terms are integrable: locally v^3 is integrable from L4,
pi v from L2 pressure and local L2 velocity, and the dissipation from
energy. Hence the required local energy inequality holds for almost
every time in the open cylinder. The suitable definition asks for no
equation or initial value beyond its upper time. In particular the
extra pressure condition belongs to suitability for Proposition 3.2;
it is not falsely added as a separate hypothesis of Theorem 1.5.

7. The tail argument prevents spatial escape uniformly near the endpoint.

Global spacetime integrability of v^4 and pi^2 implies F(R)->0 by
dominated convergence. Fixing r with (8r)^2<delta/2 ensures every
cylinder centered at s0>=delta has its lower endpoint strictly positive.
Centers beyond R+8r give cylinders outside B_R.

The volume exponents are correct: L4 to L^(12/5) contributes |B|^(1/6),
then its fourth power gives |B|^(2/3); L2 to L^(6/5) contributes
|B|^(1/3), whose square gives the same factor. Dividing by (8r)^3
gives |B1|^(2/3)(8r)^(-1) F(R). Thus one sufficiently large R makes
C+D small for every permitted center and upper time.

Under x=x0+r y, s=s0+r^2 tau, V=r v and P=r^2 pi, each of C and D
is invariant and the input cylinder becomes Q8. Proposition 3.2 gives
a uniform bound C0/r at its central upper point. For s0<Sstar the
continuous representative agrees with the existing classical velocity.
Only these earlier values are needed for the contradiction; no global
weak extension or endpoint solution is introduced. Constants are uniform
in center and upper time for this fixed finite-lifespan argument.

8. A finite spatial cover supplies the remaining endpoint bound.

Fixing rho with rho^2<Sstar/2 puts every Q_rho(x0,Sstar) strictly after
the initial time. Restriction to a ball preserves the finite global
Lorentz bound; the parabolic velocity rescaling preserves its critical
spatial exponent. All other hypotheses were established in item 6.
Theorem 1.5 therefore bounds the velocity through the upper boundary
on each smaller closed cylinder. Finitely many radius-rho/2 balls
cover the remaining closed spatial ball, and their finitely many
bounds have a finite maximum on a common final time interval.

The exterior bound and this finite-cover bound give global Linfinity
velocity near Sstar. Compact-classical LOCAL bounds handle earlier
times. Energy then gives ||v||3^3<=||v||infinity||v||2^2 uniformly up
to Sstar. Undoing viscosity normalization supplies exactly the uniform
L3 bound excluded by canonical CONTINUATION at finite Tstar.

For RF-q, choose H>Tstar at a hypothetical finite endpoint. Its single
bound transfers to the entire classical interval by item 3 and invokes
the preceding adapter. Consequently Tstar=infinity. Canonical LOCAL
and ENERGY then supply original unforced NS, the specified initial
datum, normalized smooth pressure and velocity through time zero,
and the required initial-energy bound on every finite horizon.

9. The producer accounting does not claim the missing estimate.

Multiplying the exact paired L2 energy identity by
N_j^(q/2)||e_j||2^(q-2) yields the stated Wq identity, including at
zero error, since the qth power of the Hilbert norm is differentiable
there for q>=3. The q-2 factor and the strain term are retained.
Schwartz initial shell decay makes Wq_M(0) uniformly finite.

Integrating to each t<=H and using the proposed bound with coefficient
one in front of nu integral Dq_M yields
Wq_M(t)<=Wq_M(0)+q C. Thus strict absorption is unnecessary for this
supremum output, while every upper time and uniformity in M are essential.
The candidate explicitly leaves this estimate unproved. RF-CUBE implies
RF-q for fixed q>3 by the elementary ell3-to-ellq inclusion; this does
not prove that actual NS increments satisfy either certificate.

REPLACEMENT ARGUMENT: none required.

CONDITIONAL SUFFIX THAT SURVIVES:

RF-q for any ONE fixed finite q>3, all admissible Schwartz data, positive
viscosities and finite horizons, on the accepted faithful dyadic R3
family, implies the original NS-R3 conclusion. Neither q varying with
cutoff nor bounded weak L3 at q=infinity is substituted for this premise.

UNNECESSARY DEPENDENCIES:

Global weak-solution construction, weak--strong uniqueness, approximate
pressure limits, uniform Lorentz convergence in time, bounds uniform
as q tends to infinity, and explicit quantitative dependence of the
interior covering constants are unnecessary. Only the velocity case
k=0 of Proposition 3.2 is used. The elementary synthesis proof actually
permits q>=2, but the reviewed new consumer uses fixed 3<q<infinity;
no range extension is needed or promoted.

NON-CLAIMS:

This scoped integration PASS does not prove RF-q, its signed-production
bound, NS-R3 unconditionally, a new singularity restriction, a nonlinear
response estimate, or novelty. It is not an independent reproof of Phuc's
regularity theory or the canonical interfaces and is not formal
verification. No research state, manuscript, canonical graph or proof
candidate was edited. Only the assigned audit file was written.

REOPENING CONDITION:

Any change to fixed finite q, Lorentz normalization, synthesis constants,
support/quantifier assumptions, the pressure and suitable-class argument,
closed-cylinder source conclusion, or the global-tail/continuation
composition requires a revised frozen review. Terminal promotion still
requires an actual arbitrary-data RF-q producer and independent audit.

## Final status metadata

After the passing review, the controller changed only review-status text
in the proof introductions and synthesis handoff. The complete reversible
delta is below; there is no change to a mathematical statement or source
hypothesis. Reversing it reconstructs the exact frozen review hashes above.
This is controller metadata work, not a new independent mathematical review.

This is a zero-context unified delta (apply with `--unidiff-zero`).

```diff
--- reviewed/2026-09-07-lorentz-synthesis.md
+++ final/2026-09-07-lorentz-synthesis.md
@@ -4 +4 @@
-independent synthesis review passed, integrated review pending.
+independent synthesis review passed, independent integration review passed.
@@ -68,2 +68,2 @@
-independent component review passed; the combined implication is
-subject to final integration review and remains conditional on RF-q. q=infinity is NOT being used as a continuation
+independent component and full integration reviews passed. The combined
+implication remains conditional on RF-q. q=infinity is NOT being used as a continuation
--- reviewed/2026-09-07-lorentz-continuation.md
+++ final/2026-09-07-lorentz-continuation.md
@@ -4 +4 @@
-and adapter review passed, integrated review pending. No canonical graph
+and adapter review passed, independent integration review passed. No canonical graph
```

Final 2026-09-07-lorentz-synthesis.md SHA256:
`ba075437818bbe5b1ae59e5bbc87bf52889e6455dab5a78b469d01bec809eb38`.

Final 2026-09-07-lorentz-continuation.md SHA256:
`33be1a9aa36405fb9572b22d552cbb2e7bb1ded95427843cb58948d1fd90df36`.
